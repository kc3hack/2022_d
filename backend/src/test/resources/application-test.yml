datasources:
  default:
    url: ${DB_TEST_URL}
    driverClassName: org.postgresql.Driver
    username: test
    password: test
    dialect: postgres
    maximum-pool-size: 3
flyway:
  datasources:
    default:
      enabled: true
